
CREATE TABLE tpotusa
 (name TEXT, id INTEER PRIMARY KEY);
 INSERT INTO tpotusa VALUES ("basitar", 1);
etc.

TEXT
, INTEGER, REAL, NUMERIC, BLOB
blob means no suggested type, numeric defaults to int but can be float

Columns can be given a PRIMARY KEY attribute, every entry in that column has to be unique and can't be NULL.
Columns can be given a NOT NULL attribute, denoting that no entry can be NULL.

SELECT * FROM <tablename>;
show all fields for each entry in table tablename

SELECT name FROM <tablename>;
SELECT name FROM <tablename> WHERE id=0;

$ sqlite3 <dbfile>
Add a dot to the start of SQLite shell commands